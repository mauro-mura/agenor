jentic:
  runtime:
    name: "test-runtime"
    environment: "test"
    properties:
      testMode: "true"
      timeout: "30s"

  agents:
    autoDiscovery: true
    scanPackages:
      - "dev.agenor.examples"
    properties:
      maxConcurrent: "10"
